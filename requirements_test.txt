# pytest-homeassistant-custom-component pins pytest, pytest-cov and
# pytest-asyncio internally; we keep them in this file as version floors
# so pip rejects any environment where the security CVE patches haven't
# landed (e.g. CVE-2025-71176 on pytest 9.0.0-9.0.2).
pytest>=9.0.3
pytest-cov>=7.0.0
pytest-asyncio>=1.3.0
pytest-homeassistant-custom-component>=0.13.317
aioresponses>=0.7.8

# Runtime dependencies declared in custom_components/eaux_marseille/manifest.json.
# Mirrored here so the CI test environment has them available without
# pulling them in via Home Assistant's full manifest install.
tenacity>=9.1.4
