# pytest-homeassistant-custom-component pulls in pytest, pytest-cov and
# pytest-asyncio with strict version pins of its own. We don't duplicate
# those pins here — pip resolves to whatever the upstream package wants,
# which keeps us aligned with the version Home Assistant core itself
# tests against.
pytest-homeassistant-custom-component>=0.13.339
aioresponses>=0.7.9

# Runtime dependencies declared in custom_components/eaux_marseille/manifest.json.
# Mirrored here so the CI test environment has them available without
# pulling them in via Home Assistant's full manifest install.
tenacity>=9.1.4
