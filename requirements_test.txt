pytest>=9.0.3
pytest-asyncio>=1.3.0
aioresponses>=0.7.8

# pytest-homeassistant-custom-component pins to the Python version HA itself
# uses. >=0.13.317 requires Python 3.14, while older versions still support
# 3.13. Use environment markers so CI installs the correct one for each
# Python in the matrix.
pytest-homeassistant-custom-component>=0.13.316,<0.13.317; python_version < "3.14"
pytest-homeassistant-custom-component>=0.13.325; python_version >= "3.14"

# Runtime dependencies declared in custom_components/eaux_marseille/manifest.json.
# Mirrored here so the CI test environment has them available without
# pulling them in via Home Assistant's full manifest install.
tenacity>=8.0
